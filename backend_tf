
terraform {
  backend "s3" {
    bucket       = "terraform-state-repo-test"
    key          = "ecs/dev/terraform.tfstate"
    region       = "eu-west-2" ####
    use_lockfile = true
    encrypt      = true
  }
}